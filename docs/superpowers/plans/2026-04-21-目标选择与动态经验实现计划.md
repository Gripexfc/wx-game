# 目标选择与动态经验 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现统一的目标选择界面、首页承诺卡升级，以及基于坚持天数和心情值的动态经验结算。

**Architecture:** 先通过测试锁定目标模型和 XP 公式，再改造 `GoalManager` 与 `completeGoal` 数据链路，最后接入 `OnboardingPage` 与 `HomePage` 的统一 Canvas 目标选择界面。旧 `ActionSheet`/旧 XP 字段不再作为约束，所有主要逻辑统一收敛到新的目标模型与 UI 状态中。

**Tech Stack:** 微信小游戏 Canvas UI、CommonJS 模块、Node 直跑测试脚本

---

### Task 1: 锁定目标模型与 XP 规则

**Files:**
- Modify: `js/__tests__/GoalManager.test.js`
- Modify: `js/__tests__/GameCompleteGoal.test.js`
- Test: `node js/__tests__/GoalManager.test.js && node js/__tests__/GameCompleteGoal.test.js`

- [ ] **Step 1: 写失败测试，覆盖新目标字段、streak 规则与 XP 公式**
- [ ] **Step 2: 运行测试，确认因缺少新逻辑而失败**
- [ ] **Step 3: 最小实现 `GoalManager` 新字段、preview XP、streak 更新接口**
- [ ] **Step 4: 运行测试，确认新规则通过**

### Task 2: 重构完成目标数据链路

**Files:**
- Modify: `js/main.js`
- Modify: `js/__tests__/main-stub.js`
- Modify: `js/WishManager.js`
- Test: `node js/__tests__/GameCompleteGoal.test.js && node js/__tests__/WishManager.test.js`

- [ ] **Step 1: 写失败测试，覆盖 `completeGoal` 的动态 XP、心愿叠加、streak 更新**
- [ ] **Step 2: 运行测试，确认失败原因正确**
- [ ] **Step 3: 最小实现新 XP 结算与目标状态更新**
- [ ] **Step 4: 运行测试，确认通过**

### Task 3: 接入统一目标选择 UI

**Files:**
- Create: `js/ui/GoalPickerOverlay.js`
- Modify: `js/ui/OnboardingPage.js`
- Modify: `js/ui/HomePage.js`
- Modify: `js/main.js`

- [ ] **Step 1: 为 UI 状态切换补最小支撑接口**
- [ ] **Step 2: 实现统一目标选择 overlay 的绘制与点击命中**
- [ ] **Step 3: 首页空槽位与首次进入接入统一 picker**
- [ ] **Step 4: 手动验证首次进入、首页添加、目标完成反馈**

### Task 4: 收尾验证

**Files:**
- Test: `node js/__tests__/GoalManager.test.js && node js/__tests__/WishManager.test.js && node js/__tests__/PetStateManager.test.js && node js/__tests__/GameCompleteGoal.test.js`

- [ ] **Step 1: 运行全量相关测试**
- [ ] **Step 2: 检查改动文件 lint / 运行时明显问题**
- [ ] **Step 3: 修复验证中暴露的问题**
